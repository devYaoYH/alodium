{
  "_comment": "Typed tool surface for agent consumers (app.toml [service].mcp). The registry serves this as MCP discovery. Tools are the agent-facing subset of openapi.yaml — usually fewer, coarser, and named for intent. Delete the mcp line from app.toml instead of shipping an empty file.",
  "tools": [
    {
      "name": "echo",
      "description": "Example tool — replace with the app's real agent surface.",
      "endpoint": { "method": "POST", "path": "/v1/echo" },
      "input_schema": {
        "type": "object",
        "properties": {
          "message": { "type": "string", "description": "Text to echo back" }
        },
        "required": ["message"]
      },
      "readonly": true
    }
  ]
}
