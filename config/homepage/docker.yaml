# Live container status for the dashboard — via docker-proxy, the read-only
# socket proxy (CONTAINERS=1, POST=0) on the internal `dockerwatch` network.
# The raw socket never enters this (routed) container.
sovereign-node:
  host: docker-proxy
  port: 2375
